import { styled } from 'styled-components';
import Header from '../components/header/Header';

const FavoritePageBlock = styled.div``;

const FavoritePage = () => {
  return (
    <FavoritePageBlock>
      <Header />
    </FavoritePageBlock>
  );
}

export default FavoritePage;
